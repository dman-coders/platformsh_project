id: platformsh_project_add_metric_action
label: 'Add Metric'
langcode: en
status: true
dependencies:
  enforced:
    module:
      - platformsh_project
type: node
plugin: platformsh_project_add_metric_action
# No `configuration:{}` for this action.

